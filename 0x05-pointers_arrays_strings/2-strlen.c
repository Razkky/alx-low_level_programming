#include <stdio.h>
#include <string.h>

/**
 * _strlen - get length of the a string
 * @s: string to check its length
 *
 * Return: length of string s
 */

int _strlen(char *s)
{
	int len;

	len = strlen(s);
	return (len);
}
